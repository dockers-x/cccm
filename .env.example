# Claude Code Config Manager Environment Variables

# Admin password for web interface access
# IMPORTANT: Change this in production!
ADMIN_PASSWORD=admin123

# Database configuration
# Path where the SQLite database file will be stored
DATABASE_PATH=./config.db

# Logging configuration
# Options: error, warn, info, debug, trace
RUST_LOG=info

# Timezone (optional)
TZ=UTC

# Docker-specific settings (when using docker-compose)
# These are mainly for docker-compose environment

# Port mapping (host:container)
# CCCM_PORT=3000

# Volume path for data persistence
# CCCM_DATA_PATH=./data